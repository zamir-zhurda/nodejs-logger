{
  "name": "nodejs-logger", 
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "build": "tsc && cp -r dist/ ./",
    "test": "npm run build && vitest --run --reporter verbose"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "typescript": "^5.0.2",
    "vitest": "^0.9.2"
  },
  "dependencies": {
    "commander": "^10.0.0",
    "express": "^4.18.2"
  }
}
